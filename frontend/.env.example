# Mimir Frontend Environment Variables
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local or any file containing real credentials

# =============================================================================
# SUPABASE CONFIGURATION (Required)
# =============================================================================

# Your Supabase project URL
# Found in: Supabase Dashboard > Settings > API > Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous/public key (safe to expose to client)
# Found in: Supabase Dashboard > Settings > API > Project API keys > anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase service role key (NEVER expose to client)
# Found in: Supabase Dashboard > Settings > API > Project API keys > service_role
# This key bypasses RLS - use only in server-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# AI / CLAUDE CONFIGURATION (Required for AI features)
# =============================================================================

# Anthropic Claude API key
# Get from: https://console.anthropic.com/
CLAUDE_API_KEY=your-claude-api-key-here

# =============================================================================
# BACKEND SERVICES (Required for full functionality)
# =============================================================================

# Manim worker URL for mathematical animations
# Default: http://localhost:8001 (development)
# Production: Set to your deployed Manim worker URL (must use HTTPS)
MANIM_WORKER_URL=http://localhost:8001

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# OpenAI API key (for video transcription via Whisper)
# Get from: https://platform.openai.com/
# OPENAI_API_KEY=your-openai-api-key-here

# =============================================================================
# SECURITY NOTES
# =============================================================================
#
# 1. NEVER commit credentials to version control
# 2. Use different keys for development and production
# 3. Rotate keys immediately if they are exposed
# 4. Service role key should only be used in server-side code
# 5. Enable Row Level Security (RLS) on all Supabase tables
# 6. Set up proper CORS origins in production
#
# For production deployment:
# - Use environment variables in your hosting platform (Vercel, etc.)
# - Enable HTTPS for all endpoints
# - Configure proper CORS origins
# - Set up rate limiting at the infrastructure level
