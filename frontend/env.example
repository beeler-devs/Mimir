# Mimir Frontend Environment Variables
# INSTRUCTIONS: Copy this file to .env.local and fill in your actual credentials
# 
# Command: cp env.example .env.local
# Then edit .env.local with your actual values

# Supabase Configuration (Public keys - safe for browser)
# Get these from: https://supabase.com → Your Project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Claude AI API Key (Server-side only - KEEP SECRET!)
# Get this from: https://console.anthropic.com → API Keys
# Note: This is NOT exposed to the browser (no NEXT_PUBLIC_ prefix)
CLAUDE_API_KEY=sk-ant-your-key-here

# Manim Worker URL (for local development)
MANIM_WORKER_URL=http://localhost:8001

# Backend API URLs
NEXT_PUBLIC_BACKEND_URL=http://localhost:8001
NEXT_PUBLIC_BACKEND_WS=ws://localhost:8001

# Voice Assistant (optional)
# Set to "false" to disable voice assistant feature in the UI
NEXT_PUBLIC_VOICE_ENABLED=true

