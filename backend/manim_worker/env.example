# Mimir Backend Environment Variables  
# INSTRUCTIONS: Copy this file to .env and fill in your actual credentials
#
# Command: cp env.example .env
# Then edit .env with your actual values

# Supabase Configuration (Service Role Key - KEEP SECRET!)
# Get these from: https://supabase.com → Your Project → Settings → API
# ⚠️ WARNING: Use service_role key here, NOT the anon/public key
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
SUPABASE_BUCKET_NAME=animations

# Server Configuration
PORT=8001

# Manim Code Generation Model Selection
# Complexity score threshold for using Sonnet (0.0-1.0, default: 0.5)
# Scores >= threshold use Sonnet, scores < threshold use Haiku
MANIM_MODEL_THRESHOLD=0.5

# Force a specific model (optional, for testing/override)
# Options: "haiku" or "sonnet" (leave empty for automatic selection)
# MANIM_FORCE_MODEL=

# Default model if assessment fails (default: "haiku")
MANIM_DEFAULT_MODEL=haiku

# Model to use for code repair (default: "claude-3-5-haiku-20241022")
# This model is used when generated code needs to be fixed
# MANIM_REPAIR_MODEL=claude-3-5-haiku-20241022

# Model to use for chat responses (default: "claude-sonnet-4-5")
# Set this in the backend/.env file (not in manim_worker/.env)
# CHAT_MODEL=claude-sonnet-4-5

